// will you make it codewars

const noFuel = (distanceToPump, mpg, fuelLeft) => {
    return mpg * fuelLeft >= distanceToPump
   if (noFuel>1){
     return true
     } else{
       return false
     }
 };